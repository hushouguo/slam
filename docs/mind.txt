单线程轻量进程
shard gate
shard center
shard scene

                                  center
gate                         scene
gate                         scene
gate                         scene
gate                         scene

gate: 可在同一台物理机器上绑定同个端口，多进程并发
center: 单线程来处理全局数据维护和登录注册
scene: 可在同台物理机上自增端口，spawn多个进程, 每个进程是单独的sceneserver

单线程逻辑处理起来简单快速
利用多进程并发来支持多核处理
center的注册和登录考虑利用多核

